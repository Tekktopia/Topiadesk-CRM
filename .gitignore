# Dependencies
node_modules/
.pnpm-store/

# Local Claude Code session state (not project content)
.claude/

# Build outputs
dist/
build/
.next/
.turbo/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example
secrets/
*.pem
*.key
!infra/traefik/certs/.gitkeep

# Prisma
packages/db/generated/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Test / coverage
coverage/
playwright-report/
test-results/
# Playwright auth storage state + persisted TOTP seeds (real session tokens
# and MFA secrets for the local/CI Keycloak instance — never commit these)
frontend/web/e2e/.auth/

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Docker volumes mounted locally (dev only)
infra/postgres/data/
infra/minio/data/
