# Dependencies
node_modules/
.pnpm-store/

# Local Claude Code session state (not project content)
.claude/
.tmp-screenshots/

# Build outputs
dist/
build/
.next/
.turbo/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example
secrets/
*.pem
*.key
!infra/traefik/certs/.gitkeep

# Prisma
packages/db/generated/
packages/db-platform/generated/

# training/export/convert_to_onnx.sh clones this at repo root, per its own
# header comment — external conversion tooling, not project content.
/transformers.js-tooling/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Test / coverage
coverage/
playwright-report/
test-results/
# Playwright auth storage state + persisted TOTP seeds (real session tokens
# and MFA secrets for the local/CI Keycloak instance — never commit these)
frontend/web/e2e/.auth/

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Docker volumes mounted locally (dev only)
infra/postgres/data/
infra/minio/data/

# Generated deliverables (BRD/TRD/API PDFs) and CONFIDENTIAL security
# reports (pentest + remediation). Kept out of version control — the pentest
# documents describe a live weakness and must not land in the repo history.
docs/deliverables/
