import { ScheduledReportsView } from './scheduled-reports-view';

export const metadata = {
  title: 'Scheduled reports',
};

export default function ScheduledReportsPage() {
  return <ScheduledReportsView />;
}
