import { TriggerBuilderView } from '../../_components/trigger-builder-view';

export const metadata = {
  title: 'New Trigger',
};

export default function NewTriggerPage() {
  return <TriggerBuilderView />;
}
