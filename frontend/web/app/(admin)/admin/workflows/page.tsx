import { WorkflowsListView } from '../_components/workflows-list-view';

export const metadata = {
  title: 'Workflows',
};

export default function WorkflowsPage() {
  return <WorkflowsListView />;
}
