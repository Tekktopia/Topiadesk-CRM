# TopiaDesk CRM — login theme copy overrides (keycloak.v2 base message keys).
# loginAccountTitle defaults to "Sign in to your account" in Keycloak's base
# bundle, which wraps to two ragged lines in the card at its current width
# (23.5rem) — shorter, warmer copy fixes the wrap and reads better as a
# product greeting rather than a generic auth-form label.
loginAccountTitle=Welcome back
